// Forest App Functionality\n// 1. Timer Functionality\nlet timer;\nlet timerRunning = false;\nlet timeRemaining = 0;\n\nfunction startTimer(duration) {\n    timeRemaining = duration;\n    timerRunning = true;\n    timer = setInterval(() => {\n        if (timeRemaining > 0) {\n            timeRemaining--;\n        } else {\n            clearInterval(timer);\n            timerRunning = false;\n            plantTree();\n        }\n    }, 1000);\n}\n\nfunction stopTimer() {\n    clearInterval(timer);\n    timerRunning = false;\n}\n\nfunction resetTimer() {\n    stopTimer();\n    timeRemaining = 0;\n}\n\n// 2. Tree Management\nlet treesPlanted = 0;\nlet trees = [];\n\nfunction plantTree() {\n    treesPlanted++;\n    trees.push({ id: treesPlanted, age: 0 });\n    notifyUser(`You planted a tree! Total trees: ${treesPlanted}`);\n}\n\n// 3. Coin Management\nlet coins = 0;\n\nfunction earnCoins(amount) {\n    coins += amount;\n    notifyUser(`You earned ${amount} coins! Total coins: ${coins}`);\n}\n\n// 4. Achievements\nlet achievements = [];\n\nfunction unlockAchievement(title) {\n    achievements.push(title);\n    notifyUser(`Achievement unlocked: ${title}`);\n}\n\n// 5. Notifications\nfunction notifyUser(message) {\n    console.log(message); // For now, we print to console\n}\n\n// Example Usage\nstartTimer(10); // Start a 10-second timer\n